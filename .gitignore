__pycache__/
*.pyc
.vscode/

# Claude AI (project-level AI assistance config — not part of the node)
.claude/
CLAUDE.md
HANDOFF.md
skills-lock.json
AI_RULES.md
ARCHITECTURE.md
PROJECT_SPEC.md
PROJECT_STATE.md
TASKS.md

# Local gallery index (SQLite WAL, thumbs, config with folder roots — do not publish)
gallery_data/

# Prompt Library V2 local database (SQLite WAL — personal, do not publish)
prompt_library_v2_data/

# Prompt Library V2 design/working notes (kept local)
prompt_library_v2_requirements.md
prompt_library_v2_llm_plan.md

# Secrets / local env (if you add API keys later)
.env
.env.*

# User prompt library presets (personal — keep locally only)
prompt_library/*
!prompt_library/.gitkeep

# Tag DB snapshot data (large SQLite files + settings — do not publish)
tagdb_data/

# Author-side dataset build output (uploaded to a GitHub Release, not committed)
dist/